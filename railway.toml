[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn swinedesk.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
